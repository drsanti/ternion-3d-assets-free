## Guide:

Herr I'll guide you how to do:

- List folers in the `src/assets/models/downloads`
- Loop of ver the list storing the folders
- Each folder, looking for the \*.json
- Read and parse the content of the \*.json (save/append them in an object, update the table later)
- Update the table with the data

## Features:

The metadata preview must be updated wheb the row is selected in the table.

- Remove the columns Date and Time

- Put the metada preview in the collabsible card

- Create a new card add it above the metedata card, and Design model details UI elements to display model information

- Each folder has thumbnail image (*.webp, or other types of image file). Generally the thumbnail image is saved in the same location of the json file. If the folder contains the thumbnail image, show it in the model details card 

